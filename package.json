{
  "name": "mobile-spec-brain",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.30.1",
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"]
  },
  "scripts": {
    "build": "pnpm --recursive run build",
    "check": "pnpm build && pnpm --recursive run check",
    "test": "pnpm build && pnpm --recursive run test",
    "mobile-spec-brain": "pnpm --filter @mobile-spec-brain/cli start"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
