# ADR-001: Evidence before specification

Derived specifications are built from provenance-bearing evidence rather than directly from raw content. This preserves explainability, supports multiple interpretations, and allows materialized outputs to be rebuilt.
