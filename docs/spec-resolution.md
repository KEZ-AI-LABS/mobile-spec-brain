# Spec resolution

Specifications are selected valid Claims, not editable fixed-schema records. Resolution considers authority, extraction confidence, freshness, lifecycle, qualifiers, and temporal validity independently. When Evidence is insufficient or contradictory without a policy winner, the valid answer is `UNKNOWN`.

Authority is profile-configured by specification domain. It is intentionally not a universal hard-coded hierarchy.
